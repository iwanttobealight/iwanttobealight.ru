<section class="postlist-wrapper">


<article class="news">
  <h2>новости моей жизни</h2>
  <ul reversed class="postlist">
    {%- for post in collections['новости-моей-жизни'] | reverse -%}
      <li class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}">
        <a href="{{ post.url | url }}" class="postlist-link">
          {% if post.data.title %}{{ post.data.title }}
          {% else %}
            <code>{{ post.url }}</code>
          {% endif %}
        </a>
        <time class="postlist-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
      </li>

    {%- endfor -%}
  </ul>
</article>

<article">
  <h2>итоги разных лет</h2>
  <ul reversed class="postlist">
    {%- for post in collections['end-of'] | reverse -%}
      <li class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}">
        <a href="{{ post.url | url }}" class="postlist-link">
          {% if post.data.title %}{{ post.data.title }}
          {% else %}
            <code>{{ post.url }}</code>
          {% endif %}
        </a>
        <time class="postlist-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
      </li>

    {%- endfor -%}
  </ul>
</article>

<article>
  <h2>остальное</h2>
  <ul reversed class="postlist">
    {%- for post in collections['another-tag'] | reverse -%}
      <li class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}">
        <a href="{{ post.url | url }}" class="postlist-link">
          {% if post.data.title %}{{ post.data.title }}
          {% else %}
            <code>{{ post.url }}</code>
          {% endif %}
        </a>
        <time class="postlist-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
      </li>

    {%- endfor -%}
  </ul>
</article>


</section>